# История изменений | API Яндекс Метрики

C заданной точностью

  1. [API отчетов](../../index.md)
  2. Группировки
  3. События
  4. Поведение
  5. Дата и время (у пользователя)
  6. С заданной точностью

# C заданной точностью

**Группировка** |  **Название** |  **Типы соответствий** |  **Подробное описание** |  **Расшифровка** |  **Минимальная дата для создания отчета**  
---|---|---|---|---|---  
`ym:ep:browserStartOfYear` |  Год на компьютере посетителя |  `!.` — не встречается среди значений `!=` — не равно `<` — меньше `<=` — меньше либо равно `=.` — встречается среди значений. Вы можете указать в запросе до 100 значений `==` — равно `>` — больше `>=` — больше либо равно |  Дата и время на компьютере посетителя в формате YYYY-MM-DD HH:mm:ss, округленное до начала года. |  |  2013-06-19  
`ym:ep:browserStartOfQuarter` |  Квартал на компьютере посетителя |  `!.` — не встречается среди значений `!=` — не равно `<` — меньше `<=` — меньше либо равно `=.` — встречается среди значений. Вы можете указать в запросе до 100 значений `==` — равно `>` — больше `>=` — больше либо равно |  Дата и время на компьютере посетителя в формате YYYY-MM-DD HH:mm:ss, округленное до начала квартала. |  |  2013-06-19  
`ym:ep:browserStartOfMonth` |  Месяц на компьютере посетителя |  `!.` — не встречается среди значений `!=` — не равно `<` — меньше `<=` — меньше либо равно `=.` — встречается среди значений. Вы можете указать в запросе до 100 значений `==` — равно `>` — больше `>=` — больше либо равно |  Дата и время на компьютере посетителя в формате YYYY-MM-DD HH:mm:ss, округленное до начала месяца. |  |  2013-06-19  
`ym:ep:browserStartOfWeek` |  Неделя на компьютере посетителя |  `!.` — не встречается среди значений `!=` — не равно `<` — меньше `<=` — меньше либо равно `=.` — встречается среди значений. Вы можете указать в запросе до 100 значений `==` — равно `>` — больше `>=` — больше либо равно |  Дата и время на компьютере посетителя в формате YYYY-MM-DD HH:mm:ss, округленное до начала недели. |  |  2013-06-19  
`ym:ep:browserStartOfHour` |  Час на компьютере посетителя |  `!.` — не встречается среди значений `!=` — не равно `<` — меньше `<=` — меньше либо равно `=.` — встречается среди значений. Вы можете указать в запросе до 100 значений `==` — равно `>` — больше `>=` — больше либо равно |  Дата и время на компьютере посетителя в формате YYYY-MM-DD HH:mm:ss, округленное до начала часа. |  |  2013-06-19  
`ym:ep:browserStartOfDekaminute` |  Начало декаминуты на компьютере посетителя |  `!.` — не встречается среди значений `!=` — не равно `<` — меньше `<=` — меньше либо равно `=.` — встречается среди значений. Вы можете указать в запросе до 100 значений `==` — равно `>` — больше `>=` — больше либо равно |  Дата и время на компьютере посетителя в формате YYYY-MM-DD HH:mm:ss, округленное до начала 10-минутного интервала. |  |  2013-06-19  
`ym:ep:browserStartOfMinute` |  Минута на компьютере посетителя |  `!.` — не встречается среди значений `!=` — не равно `<` — меньше `<=` — меньше либо равно `=.` — встречается среди значений. Вы можете указать в запросе до 100 значений `==` — равно `>` — больше `>=` — больше либо равно |  Дата и время на компьютере посетителя в формате YYYY-MM-DD HH:mm:ss, округленное до начала минуты. |  |  2013-06-19  
`ym:ep:browserDateTime` |  Дата и время на компьютере посетителя |  `!.` — не встречается среди значений `!=` — не равно `<` — меньше `<=` — меньше либо равно `=.` — встречается среди значений. Вы можете указать в запросе до 100 значений `==` — равно `>` — больше `>=` — больше либо равно |  Дата и время на компьютере посетителя в формате YYYY-MM-DD HH:mm:ss. |  |  2013-06-19  
  
### Была ли статья полезна?

ДаНет

Предыдущая

[Дата и время (у пользователя)](../event_paramsbehavior_/browser_date_time.md)

Следующая

[Фрагмент даты/времени](browser_date_time_partial.md)